# Small utilities for MPD, the Music Player Daemon

## queue-len
Limits the length of the queue by only keeping the last `N` elements.

Accepts the standard `MPD_HOST` and `MPD_PORT` environment variables.
